language: php

php:
  - '5.4'
  - '5.5'
  - '5.6'

sudo: false

before_install:
  - phpenv config-rm xdebug.ini

install:
  - travis_retry composer install --no-interaction

before_script:
  - make directories

script: composer unittest